{
  "name": "codecats-academy",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview-build": "vite build && vite preview",
    "deploy": "gh-pages -d dist",
    "deploy-build": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "lightningcss": "^1.21.5",
    "vite": "^4.4.0",
    "vite-plugin-html": "^3.2.0",
    "vite-plugin-lightningcss": "^0.0.5"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
